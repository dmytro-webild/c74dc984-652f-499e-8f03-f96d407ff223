import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import Tag from "@/tag/Tag";
import { getVisualEditScript } from "@/utils/visual-edit-script";
import { Raleway } from "next/font/google";



export const metadata: Metadata = {
  title: 'Syed Mohsin Ali | Digital Marketing Expert & Freelancer',
  description: 'Expert freelance digital marketing services. Building brands digitally with creative marketing strategies for international clients.',
  keywords: ["digital marketing, freelance marketing, branding, growth strategist, social media manager"],
  openGraph: {
    "title": "Syed Mohsin Ali | Freelance Marketing Expert",
    "description": "Creative marketing strategies and freelance solutions for your business growth.",
    "siteName": "Syed Mohsin Ali"
  },
};

const raleway = Raleway({
  variable: "--font-raleway",
  subsets: ["latin"],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body className={`${raleway.variable} antialiased`}>
          <Tag />
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
